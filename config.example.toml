# Sesame5 UUID. Accepts dashed or non-dashed hex.
sesame_id = "00000000-0000-0000-0000-000000000000"

# Sesame5 OS3 secret key hex. This is the 16-byte device secret saved by the Sesame SDK/app.
sesame_secret_key = "00000000000000000000000000000000"

# Nature Remo Cloud API personal access token.
nature_token = "replace-me"

# Nature Remo signal ID for "light on".
nature_light_on_signal_id = "replace-me"

cooldown_seconds = 30

# Required for daemon. History dump can override this with --delete-after-read.
# Deleting advances Sesame5's history queue but removes the record before the Sesame app can fetch it.
delete_history_after_read = false

# Fill this after collecting history-dump samples.
# [touch_pro_match]
# Patterns are matched against the event body after the 4-byte record ID.
# For Sesame5 BLE unlock history, the final 16 bytes can be used as the
# stable source tag after confirming it across multiple Touch Pro samples.
# contains_hex = ["replace-with-32-hex-character-touch-pro-source-tag"]
